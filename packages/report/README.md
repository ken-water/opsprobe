# @opsprobe/report

Report view models and export logic for OpsProbe.

Current scope:

- build a report-friendly view model from one or more inspection runs
- group results by host and severity
- expose summary counts for status and severity
- provide a stable data shape for future HTML and PDF exporters
