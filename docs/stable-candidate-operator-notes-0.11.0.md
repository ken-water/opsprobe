# Stable Candidate Operator Notes: 0.11.0 Draft

This draft is based on the current `0.11.0` evidence line and the latest clean-user-profile validation artifacts.

It is not the final `1.0.0` operator sign-off. Its purpose is to turn the current evidence into reviewable operator-facing conclusions before Issue `47` is resumed.

## Candidate Metadata

- Candidate version: `0.11.0`
- Validation date: `2026-06-08`
- Validator: Codex-assisted repository validation
- Machine or VM label: local development environment
- Operating system: local Linux workspace
- PostgreSQL binaries available in `PATH`: no (`postgres`, `pg_ctl`, and `initdb` missing in the captured run)
- Validation home/profile path: temporary clean `HOME` generated by `./scripts/validate-clean-user-profile.sh`

## 1. Install And Bootstrap

### Local prerequisites seen by the operator

- Node / npm: present
- Rust / cargo: present
- Tauri dependencies: build path passed during recent release validation, but this draft did not re-run desktop packaging directly
- PostgreSQL binaries: not present in the captured `0.11.0` draft evidence

### First-run status result

- Command used: `HOME="<tmp>" npm run local-service:status`
- `~/.opsprobe` was created automatically under the temporary profile
- Missing prerequisites were surfaced through status checks instead of failing silently
- `recoveryActions` appeared and gave a next-step path even when PostgreSQL binaries were unavailable

### Bootstrap notes

- Command used:
  - `HOME="<tmp>" npm run local-service:postgres-bootstrap`
  - `HOME="<tmp>" npm run local-service:postgres-start`
  - `HOME="<tmp>" npm run local-service:status`
- In the current captured draft environment, PostgreSQL bootstrap could not be fully proven because binaries were missing
- The failure mode is now reviewable: the operator can see that PostgreSQL prerequisites are missing and that fallback behavior remains active
- This is better than silent failure, but it is still not enough to claim clean-machine bootstrap credibility for `1.0.0`

## 2. Primary Workflow

### Asset save

- Command or UI path: `assets-upsert` through the local-service CLI validation path
- Save path was successful from a clean profile
- No hidden database-specific knowledge was required for the operator to persist an asset

### Inspection preview or run

- Command or UI path: `inspect-preview` through the clean-profile validation script
- The operator receives a structured preview result
- This is credible for a draft validation path, but still CLI-driven rather than a fully documented packaged desktop acceptance path

### Report export

- Command or UI path: `config-export` for migration package export is validated; report export remains stronger in smoke coverage than in this specific draft
- Output path is explicit in the command flow
- The operator can tell where the export artifact is written

## 3. Stop / Restart / Recovery

### Stop flow

- Command or UI path: `npm run local-service:stop`
- The result is explicit and no longer relies on implicit PID assumptions
- The current behavior is honest enough to review for `1.0.0`, even if supervision remains best-effort

### Restart flow

- Command or UI path: `node --experimental-strip-types ./apps/local-service/src/main.ts restart`
- The product does not claim the background process is already healthy after restart preparation
- The next step remains explicit: start the service again and review recovery guidance

### Recovery guidance

- Current captured `recoveryActions` explain start-or-restart, PostgreSQL bootstrap, and other repair steps depending on environment state
- This is a meaningful improvement over earlier opaque status behavior
- For `1.0.0`, this area looks documentable, but still not equivalent to hardened service-manager supervision

## 4. Backup And Machine Replacement

### Backup understanding

- The operator can now identify the main backup scopes:
  - `~/.opsprobe/data`
  - `~/.opsprobe/runtime`
  - exported config package
- The distinction between runtime state, reports, and migration packages is documented more clearly than before

### Migration package understanding

- The export package is produced successfully during the clean-profile flow
- Source-machine provenance is visible in exported config packages
- Post-import credential rebind and schedule trust expectations are documented, but still rely on the operator to perform the steps manually

## 5. Accepted Limits Or Blockers

List anything that still blocks a credible stable release:

- Blocker 1: clean-machine bootstrap with managed PostgreSQL is not yet proven on a machine where PostgreSQL binaries are actually present and documented from an operator perspective
- Blocker 2: current evidence is still mostly CLI and repository-driven; a packaged desktop-oriented stable candidate walk-through is not yet captured strongly enough

List any limit that might be acceptable for `1.0.0` if documented honestly:

- Accepted limit candidate 1: runtime supervision can remain process-based and best-effort if the limitations are made explicit in release notes and operator docs
- Accepted limit candidate 2: credential rebind and SSH verification may remain manual if recurring schedules stay blocked until verification succeeds and the workflow is documented clearly

## 6. Stable Decision Recommendation

Choose one:

- Continue another pre-stable issue

Reasoning:

- The repository now has a much stronger evidence chain than it did before `0.11.0`
- However, the captured evidence still shows missing PostgreSQL binaries in the clean-profile draft environment, which means the “new user can install and bootstrap the managed runtime credibly” claim is not yet defensible for `1.0.0`
- The next step should be one more evidence-focused slice that captures packaged or near-packaged operator notes on a machine where the managed PostgreSQL path is actually available

## Related Evidence

- [Clean User Profile Validation](./clean-user-profile-validation.md)
- [Stable Candidate Operator Notes Template](./stable-candidate-operator-notes.md)
- [Stable Review Record](./stable-review-record.md)
