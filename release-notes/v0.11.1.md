# OpsProbe v0.11.1

## Summary

`v0.11.1` turns the active `0.11.x` line into a stricter checkpoint flow while continuing to tighten the inspect and reports operator path.

## Included Milestones

- `0.11.x` stable-candidate checkpoint discipline

## Key Changes

- enforce patch-checkpoint versioning so ongoing work does not accumulate under one reused version number
- allow unreleased patch checkpoints inside the active minor line without breaking the contiguous version gate
- focus the inspect entry and report mode flow so the main operator path is easier to follow

## Scope Boundary

Included in `0.11.1`:

- development-gate and release-discipline hardening
- inspect and reports workflow tightening

Deferred to the next version:

- the final `System` workspace simplification pass
- additional stable-decision review and release-or-defer work

## Known Limits

- `0.11.1` improves version traceability, but the `System` page still carries duplicated guidance
- broader `1.0.0` decision evidence remains outside this checkpoint

## Next Target Version

- `0.11.2`
