# OpsProbe v0.12.2

## Summary

`v0.12.2` is a packaging-chain diagnosis checkpoint. It does not close Windows acceptance, but it makes the current blocker explicit: this machine cannot refresh the vendored Cargo dependency set needed to complete current-version Windows GNU packaging.

## Included Milestones

- `0.12.x` Windows packaging-chain diagnosis

## Key Changes

- record the current Windows GNU packaging failure for `0.12.2`
- update Cargo cache hydration to try both mirror and upstream crate download sources
- add a current-version Issue 57 progress note for the packaging-chain blocker

## Scope Boundary

Included in `0.12.2`:

- diagnosis of the current Windows packaging-chain blocker
- documentation of the vendoring failure mode on this machine

Deferred to the next checkpoint:

- successful current-version NSIS installer generation
- real Windows installer acceptance on a Windows-capable environment

## Known Limits

- current Windows GNU packaging for `0.12.2` is still blocked on this machine before installer generation completes
- the missing NSIS installer is currently an environment/build-chain blocker, not yet a resolved product-side path

## Next Target Version

- `0.12.3`
