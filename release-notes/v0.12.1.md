# OpsProbe v0.12.1

## Summary

`v0.12.1` is the first real evidence-refresh checkpoint on the final pre-stable line. Linux packaged artifacts and headless launch proof are now current for `0.12.1`, and Windows artifact status is also refreshed to the active version.

## Included Milestones

- `0.12.x` packaged acceptance evidence refresh

## Key Changes

- rebuild Linux `.deb`, `.rpm`, and `.AppImage` artifacts for `0.12.1`
- refresh packaged preflight and AppImage launch-smoke records for `0.12.1`
- refresh Windows artifact and Wine-blocker records for `0.12.1`
- add current-version Issue 57 progress and acceptance notes

## Scope Boundary

Included in `0.12.1`:

- current-version Linux packaging evidence refresh
- current-version Windows artifact status refresh
- issue-record updates for the active pre-stable line

Deferred to the next checkpoint:

- real operator-facing Linux packaged acceptance
- real Windows installer acceptance
- reopened `1.0.0` decision after those proofs exist

## Known Limits

- Linux evidence is current but still headless; human packaged acceptance is still open
- Windows desktop binary exists for `x86_64-pc-windows-gnu`, but the NSIS installer is still missing and Wine is not installed on this machine

## Next Target Version

- `0.12.2`
